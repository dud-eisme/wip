create a new branch:
```
git switch -c <branch-name>
```
adding files to branch:
```
git add <folder-name>
```
commit:
```
git commit -m "<commit-info>"
```
pushing to branch:
```
git push origin <branch-name>
```
